import styled from "styled-components";

const HeaderContainer = styled.header`
  font-size: 36px;
  font-weight: bold;
  margin: 80px 0;
`;

export const Header = () => {
  return <HeaderContainer>Pomodoro</HeaderContainer>;
};
